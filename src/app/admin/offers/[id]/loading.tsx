import { OfferFormSkeleton } from "@/components/admin/offer-skeletons";

/** Shown while an offer loads, so opening one answers at once. */
export default function EditOfferLoading() {
  return <OfferFormSkeleton />;
}
