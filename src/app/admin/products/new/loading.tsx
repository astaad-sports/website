import { ProductEditorSkeleton } from "@/components/admin/product-editor-skeleton";

/** Shown while Add product opens. */
export default function NewProductLoading() {
  return <ProductEditorSkeleton />;
}
